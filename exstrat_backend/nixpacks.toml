# Configuration Nixpacks pour Railway
[phases.setup]
nixPkgs = ['nodejs-20']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build', 'npx prisma generate']

[start]
cmd = 'npm run start:prod'

